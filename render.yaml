services:
  - type: web
    name: movierec
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "streamlit run rec.py --server.port $PORT --server.address 0.0.0.0"